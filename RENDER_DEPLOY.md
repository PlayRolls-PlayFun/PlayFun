# Деплой на Render

## 🚀 Пошаговая инструкция для Render

### 1. Подготовка репозитория
1. Загрузите все файлы на GitHub (кроме node_modules и .env)
2. Убедитесь что есть файлы:
   - `package.json`
   - `server.js` 
   - `index.html`
   - `script.js`
   - `styles.css`

### 2. Создание сервиса в Render
1. Зайдите на https://render.com
2. Нажмите "New" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий с сайтом

### 3. Настройки деплоя
- **Name**: `playfun-website` (или любое другое)
- **Environment**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Instance Type**: `Free` (для начала)

### 4. Переменные окружения (опционально)
Если хотите использовать Exaroton API:
- `EXAROTON_API_TOKEN` = ваш токен
- `EXAROTON_SERVER_ID` = ID сервера

Если не добавите - будет использоваться McStatus.io API (работает без настройки).

### 5. Деплой
1. Нажмите "Create Web Service"
2. Render автоматически:
   - Скачает код из GitHub
   - Выполнит `npm install`
   - Запустит `npm start`
3. Через 2-3 минуты сайт будет доступен

### 6. Получение URL
После деплоя вы получите URL вида:
`https://playfun-website.onrender.com`

## ✅ Что работает автоматически:

- ✅ **Статус сервера** - обновляется каждые 30 секунд
- ✅ **Количество игроков** - показывает реальные данные
- ✅ **Определение "сна"** - для Exaroton серверов
- ✅ **Адаптивный дизайн** - работает на мобильных
- ✅ **Автоматические обновления** - при изменении кода в GitHub

## 🔧 Настройка автообновлений

1. В настройках Render включите "Auto-Deploy"
2. При каждом push в GitHub сайт будет обновляться автоматически

## 📊 Мониторинг

В панели Render вы можете:
- Смотреть логи сервера
- Мониторить использование ресурсов  
- Перезапускать сервис при необходимости

## 🆓 Бесплатный план

Render предоставляет:
- 750 часов в месяц бесплатно
- Автоматический сон при неактивности
- SSL сертификат
- Кастомный домен (в платных планах)