# PlayFun Minecraft Server Website

Сайт для Minecraft сервера PlayFun с автоматическим отображением статуса сервера в реальном времени.

## 🚀 Возможности

- ✅ Автоматическое обновление статуса сервера каждые 30 секунд
- ✅ Отображение количества игроков онлайн
- ✅ Определение состояния "сна" для Exaroton серверов
- ✅ Темная тема дизайна
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Правила сервера
- ✅ Разделы форума

## 🛠 Технологии

- **Frontend**: HTML, CSS, JavaScript
- **Backend**: Node.js, Express
- **API**: McStatus.io для получения статуса сервера
- **Хостинг**: Совместим с Vercel, Railway, Render

## 📦 Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/ваш-username/playfun-website.git
cd playfun-website
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
```
Отредактируйте `.env` файл с вашими данными.

4. **Запустите сервер:**
```bash
npm start
```

Сайт будет доступен по адресу: http://localhost:3000

## ⚙️ Настройка

### Для Exaroton серверов:
Если у вас есть API токен Exaroton, добавьте в `.env`:
```
EXAROTON_API_TOKEN=ваш_токен
EXAROTON_SERVER_ID=ваш_server_id
```

### Для обычных серверов:
Система автоматически использует McStatus.io API для получения статуса.

## 🌐 Деплой

### Vercel (Рекомендуется)
1. Загрузите код на GitHub
2. Подключите репозиторий к Vercel
3. Добавьте переменные окружения в настройках Vercel
4. Деплой произойдет автоматически

### Railway
1. Подключите GitHub репозиторий к Railway
2. Добавьте переменные окружения
3. Деплой произойдет автоматически

### Render
1. Создайте новый Web Service в Render
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения
4. Деплой произойдет автоматически

## 📁 Структура проекта

```
playfun-website/
├── index.html          # Главная страница
├── styles.css          # Стили
├── script.js           # Клиентский JavaScript
├── server.js           # Node.js сервер
├── minecraft-ping.js   # Утилиты для пинга сервера
├── package.json        # Зависимости Node.js
├── .env.example        # Пример настроек
├── .gitignore          # Исключения для Git
└── README.md           # Документация
```

## 🔧 API Endpoints

- `GET /api/server-status` - Получить текущий статус сервера

### Пример ответа:
```json
{
  "online": true,
  "players": 5,
  "maxPlayers": 400,
  "lastUpdate": "2024-01-01T12:00:00.000Z",
  "status": "Online (McStatus API)",
  "motd": "🎄🎄PlayFun🎄🎄 [1.20.1] 🎄🎄ЗИМНИЙ ВАЙП🎄🎄"
}
```

## 🎨 Кастомизация

### Изменение адреса сервера:
В `server.js` найдите строку:
```javascript
const mcResult = await getMcStatusInfo('PlayFun.exaroton.me', 30775);
```

### Изменение интервала обновления:
В `server.js` найдите:
```javascript
setInterval(getServerStatus, 30000); // 30 секунд
```

### Изменение цветовой схемы:
В `styles.css` измените переменную:
```css
/* Текущий цвет: светло-синий */
color: #64B5F6;
```

## 📝 Лицензия

MIT License - используйте свободно для своих проектов.

## 🤝 Поддержка

Если у вас есть вопросы или предложения, создайте Issue в этом репозитории.